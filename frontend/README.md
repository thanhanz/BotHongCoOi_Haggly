# Haggly Frontend

This directory is reserved for the future React/TypeScript website. Frontend
implementation is intentionally not started yet.

The website will consume the API from `../backend` and will contain buyer,
vendor, and administrator experiences. Shared product requirements and
workflow definitions are documented at the repository root.
